# TUI Fallback Prompt

This plugin provides a secure terminal UI fallback for OSIRIS diagnostics and plugin state management.
Render all plugin status, system metrics, and logs interactively.

Inputs:
- plugin_states: JSON with plugin names and status
- system_metrics: JSON with CPU, memory, disk usage

Outputs:
- Live interactive TUI rendered in terminal

End.
