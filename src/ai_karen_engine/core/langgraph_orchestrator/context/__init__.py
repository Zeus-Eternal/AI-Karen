"""LangGraph runtime context layer.

This package owns request-time context assembly, memory/session continuity,
and runtime file/context adapters used during orchestration. It is not the
persistent context management authority.
"""
