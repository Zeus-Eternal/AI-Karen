"""
Maintenance Routes — Retired / Folded.

Legacy maintenance routes (previously used for data cleanup) have been folded into
the centralized Runtime Admin API (api_routes/runtime_admin_routes.py).

This module exists only to prevent import errors during auto-discovery.
No routes are registered.
"""

# No router — prevents auto-discovery from mounting stale endpoints.
