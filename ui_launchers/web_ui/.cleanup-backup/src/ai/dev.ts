
import { config } from 'dotenv';
config(); // This loads .env variables into process.env

// Note: AI flows have been migrated to Python backend services
// This file is kept for backward compatibility and tool functions only

console.log("\n✅ AI Karen Web UI - Backend integration mode");
console.log("🔄 AI flows are now handled by Python backend services");
console.log("🛠️  Only tool functions remain in TypeScript for compatibility\n");
