# AI Karen – Web UI (Next.js) .env example

# URL the Next.js API proxy forwards to. Defaults to http://127.0.0.1:8000.
# Set if your backend isn’t at the default address.
# NEXT_PUBLIC_BACKEND_URL=http://127.0.0.1:8000

# Enable a client-side fallback to /api/auth/dev-login when
# normal login endpoints fail. DO NOT USE IN PRODUCTION.
NEXT_PUBLIC_ENABLE_DEV_LOGIN=false

# Optional: tweak proxy behavior
# Increase per-request timeout (ms) for provider/auth/model endpoints on the UI proxy
# KAREN_WEB_API_TIMEOUT_MS=45000

# Standard Next.js flags
# NODE_ENV=development
