/**
 * Static parameters for extension pages
 *
 * Defines the static paths that should be generated during build
 */

import { generateStaticParams } from './static-params';

// Re-export generateStaticParams for Next.js static generation
export { generateStaticParams };